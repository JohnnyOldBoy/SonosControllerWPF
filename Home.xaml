<Page x:Class="SonosController.Controller"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ExpenseIt"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="ExpenseIt - Home">
    <DataGrid ItemsSource="{Binding zonePlayersOB}" Grid.IsSharedSizeScope="True">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="one" />
                        <ColumnDefinition Width="Auto" SharedSizeGroup="two" />
                    </Grid.ColumnDefinitions>
                    <!--<Image Grid.Column="1" Source="Binding iconURL"/>-->
                    <!--<TextBox Grid.Column="1" Text="{Binding Value}" />-->
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </DataGrid>
</Page>
